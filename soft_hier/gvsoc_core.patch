diff --git a/models/cpu/iss/include/cores/snitch_fast/class.hpp b/models/cpu/iss/include/cores/snitch_fast/class.hpp
index 397bcc76..0e359376 100644
--- a/models/cpu/iss/include/cores/snitch_fast/class.hpp
+++ b/models/cpu/iss/include/cores/snitch_fast/class.hpp
@@ -115,10 +115,19 @@ public:
 
     CsrReg csr_fmode;
 
+    // Mempool CSR_TRACE (0x7d0), written by mempool_start_benchmark() and
+    // mempool_stop_benchmark() to delimit a region of interest.
+    CsrReg bench_csr;
+    int64_t bench_start_cycles;
+    int64_t bench_start_time;
+    uint32_t bench_region;
+    bool bench_active;
+
 
 private:
     bool barrier_update(bool is_write, iss_reg_t &value);
     static void barrier_sync(vp::Block *__this, bool value);
+    bool bench_csr_update(bool is_write, iss_reg_t &value);
 
     vp::WireMaster<bool> barrier_req_itf;
     vp::WireSlave<bool> barrier_ack_itf;
diff --git a/models/cpu/iss/src/snitch_fast/sequencer.cpp b/models/cpu/iss/src/snitch_fast/sequencer.cpp
index b7a11e7a..1974ead3 100644
--- a/models/cpu/iss/src/snitch_fast/sequencer.cpp
+++ b/models/cpu/iss/src/snitch_fast/sequencer.cpp
@@ -69,6 +69,16 @@ void Sequencer::reset(bool active)
         this->frep_enabled = false;
         this->total_insn_count = 0;
         this->total_insn_read_count = 0;
+        // insn_count is compared against buffer.size() (unsigned) to decide whether the sequence
+        // buffer can be drained. Left uninitialized, any non-zero garbage blocks the drain forever,
+        // which then wedges the core on the next non-sequencable instruction.
+        this->insn_count = 0;
+        this->rpt_count = 0;
+        this->input_insn_is_sequence = false;
+        this->input_insn_reg = 0;
+        this->buffer.clear();
+        this->lsu_buffer.clear();
+        this->frep_buffer.clear();
     }
 }
 
diff --git a/models/cpu/iss/src/snitch_fast/snitch.cpp b/models/cpu/iss/src/snitch_fast/snitch.cpp
index 6366d004..1df8320f 100644
--- a/models/cpu/iss/src/snitch_fast/snitch.cpp
+++ b/models/cpu/iss/src/snitch_fast/snitch.cpp
@@ -21,6 +21,7 @@
 
 #include "cpu/iss/include/iss.hpp"
 #include <string.h>
+#include <inttypes.h>
 
 
 Iss::Iss(IssWrapper &top)
@@ -44,6 +45,10 @@ Iss::Iss(IssWrapper &top)
     this->barrier.register_callback(std::bind(&Iss::barrier_update, this, std::placeholders::_1,
         std::placeholders::_2));
 
+    this->csr.declare_csr(&this->bench_csr, "trace", 0x7d0);
+    this->bench_csr.register_callback(std::bind(&Iss::bench_csr_update, this,
+        std::placeholders::_1, std::placeholders::_2));
+
     this->barrier_ack_itf.set_sync_meth(&Iss::barrier_sync);
     this->top.new_slave_port("barrier_ack", &this->barrier_ack_itf, (vp::Block *)this);
 
@@ -79,6 +84,42 @@ bool Iss::barrier_update(bool is_write, iss_reg_t &value)
 }
 
 
+// This gets called when the mempool trace csr (0x7d0) is accessed.
+// mempool_start_benchmark() writes 1 to open a region of interest, mempool_stop_benchmark()
+// writes 0 to close it, at which point we report how long the region took.
+bool Iss::bench_csr_update(bool is_write, iss_reg_t &value)
+{
+    if (!is_write)
+    {
+        return true;
+    }
+
+    if (value != 0)
+    {
+        this->bench_start_cycles = this->top.clock.get_cycles();
+        this->bench_start_time = this->top.time.get_time();
+        this->bench_active = true;
+    }
+    else if (this->bench_active)
+    {
+        int64_t end_cycles = this->top.clock.get_cycles();
+        int64_t end_time = this->top.time.get_time();
+
+        printf("[BENCH] cycles=%" PRId64 " ns=%" PRId64 " start=%" PRId64 " end=%" PRId64
+            " region=%u hart=%d path=%s\n",
+            end_cycles - this->bench_start_cycles,
+            (end_time - this->bench_start_time) / 1000,
+            this->bench_start_cycles, end_cycles,
+            this->bench_region++, (int)this->csr.mhartid,
+            this->top.get_path().c_str());
+
+        this->bench_active = false;
+    }
+
+    return true;
+}
+
+
 // This gets called when the barrier is reached
 void Iss::barrier_sync(vp::Block *__this, bool value)
 {
@@ -154,6 +195,14 @@ void IssWrapper::reset(bool active)
     this->iss.decode.reset(active);
     this->iss.gdbserver.reset(active);
 
+    if (active)
+    {
+        this->iss.bench_active = false;
+        this->iss.bench_region = 0;
+        this->iss.bench_start_cycles = 0;
+        this->iss.bench_start_time = 0;
+    }
+
 #if defined(CONFIG_GVSOC_ISS_USE_SPATZ)
     this->iss.syscalls.reset(active);
     this->iss.vector.reset(active);
diff --git a/models/utils/common_cells.py b/models/utils/common_cells.py
index 3916ea4f..24a4d07d 100644
--- a/models/utils/common_cells.py
+++ b/models/utils/common_cells.py
@@ -32,3 +32,20 @@ class And(gvsoc.systree.Component):
 
     def o_OUTPUT(self, itf: gvsoc.systree.SlaveItf):
         self.itf_bind('output', itf, signature='wire<bool>')
+
+class Or(gvsoc.systree.Component):
+    def __init__(self, parent: gvsoc.systree.Component, name: str, nb_input: int=0):
+
+        super().__init__(parent, name)
+
+        self.add_sources(['utils/common_cells/or.cpp'])
+
+        self.add_properties({
+            'nb_input': nb_input
+        })
+
+    def i_INPUT(self, id: int) -> gvsoc.systree.SlaveItf:
+        return gvsoc.systree.SlaveItf(self, f'input_{id}', signature='wire<bool>')
+
+    def o_OUTPUT(self, itf: gvsoc.systree.SlaveItf):
+        self.itf_bind('output', itf, signature='wire<bool>')
diff --git a/models/utils/common_cells/or.cpp b/models/utils/common_cells/or.cpp
new file mode 100644
index 00000000..9f39a183
--- /dev/null
+++ b/models/utils/common_cells/or.cpp
@@ -0,0 +1,127 @@
+/*
+ * Copyright (C) 2020 GreenWaves Technologies, SAS, ETH Zurich and
+ *                    University of Bologna
+ *
+ * Licensed under the Apache License, Version 2.0 (the "License");
+ * you may not use this file except in compliance with the License.
+ * You may obtain a copy of the License at
+ *
+ *     http://www.apache.org/licenses/LICENSE-2.0
+ *
+ * Unless required by applicable law or agreed to in writing, software
+ * distributed under the License is distributed on an "AS IS" BASIS,
+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+ * See the License for the specific language governing permissions and
+ * limitations under the License.
+ */
+
+/*
+ * Authors: Germain Haugou, GreenWaves Technologies (germain.haugou@greenwaves-technologies.com)
+ */
+
+#include <vp/vp.hpp>
+#include <vp/itf/wire.hpp>
+
+
+class Or : public vp::Component
+{
+
+public:
+    Or(vp::ComponentConf &config);
+
+    void reset(bool active);
+
+private:
+    static void sync(vp::Block *__this, bool value, int id);
+
+    vp::Trace trace;
+
+    std::vector<vp::WireSlave<bool>> input_itfs;
+    vp::WireMaster<bool> output_itf;
+
+    std::vector<uint64_t> values;
+    int nb_values;
+    uint64_t last_value_mask;
+};
+
+
+
+Or::Or(vp::ComponentConf &config)
+    : vp::Component(config)
+{
+    traces.new_trace("trace", &trace, vp::DEBUG);
+
+    int nb_input = this->get_js_config()->get_child_int("nb_input");
+    this->input_itfs.resize(nb_input);
+
+    for (int i=0; i<nb_input; i++)
+    {
+        this->input_itfs[i].set_sync_meth_muxed(&Or::sync, i);
+        this->new_slave_port("input_" + std::to_string(i), &this->input_itfs[i]);
+    }
+
+    this->new_master_port("output", &this->output_itf);
+
+    this->nb_values = (nb_input + 63) / 64;
+
+    this->last_value_mask = ~((1 << (nb_input % 64)) - 1);
+
+    this->values.resize(this->nb_values);
+}
+
+
+
+void Or::reset(bool active)
+{
+    if (active)
+    {
+        for (int i=0; i<this->nb_values; i++)
+        {
+            this->values[i] = 0;
+        }
+
+        this->values[this->nb_values - 1] = this->last_value_mask;
+    }
+}
+
+
+
+void Or::sync(vp::Block *__this, bool value, int id)
+{
+    Or *_this = (Or *)__this;
+
+    int value_byte = id / 64;
+    int value_bit = id % 64;
+    //used for array of individual bits, in my case of OR:
+    //  should be only one entry where the first two bits are used.
+    if (value)
+    {
+        _this->values[value_byte] |= 1 << value_bit;
+    }
+    else
+    {
+        _this->values[value_byte] &= ~(1 << value_bit);
+    }
+    //Module OR should return false iff when all bits of all entries of values[] are 0,
+    //  i.e when values[i] == 0 for all i.
+    //should return true when a single bit is 1 and hence some values[i] != 0.
+
+
+    for (int i=0; i<_this->nb_values; i++)
+    {
+        if (_this->values[i] != 0)
+        {
+            _this->output_itf.sync(true);
+            return;
+        }
+    }
+    
+        _this->output_itf.sync(false);
+}
+
+
+
+extern "C" vp::Component *gv_new(vp::ComponentConf &config)
+{
+    return new Or(config);
+}
